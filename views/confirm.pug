extends ../../views/base.pug

block append scripts
  script(type="text/javascript" src="/projects/tactics-board/static/js/main.js")

block content
  div(class="position-relative container-fluid px-0")
    header(class="row p-4 g-0")
      h1(class="display-3") #{title}

    if success
      div(class="alert alert-success px-4 mb-0") #{success}
    if error
      div(class="alert alert-danger px-4 mb-0") #{error}

  div(class="row g-0 p-4")
    p(class="mb-0"): a(href=`/return?b=${back}` class="btn btn-secondary") Return
